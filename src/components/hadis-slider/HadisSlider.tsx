import { Carousel } from 'antd';
interface HadisSliderProps { }

export const HadisSlider = ({ }: HadisSliderProps) => {
    return (
        <>
            <Carousel>

            </Carousel>
        </>
    );
}